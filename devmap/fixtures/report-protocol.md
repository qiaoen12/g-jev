# 报告协议 Mock 验收

这是一组固定的模拟输入及期望结果，用于逐项重放文本 Skill；不是当前仓库开发记录，也不声称执行过真实业务测试。所有路径与结果均为 fixture。重放时先读 `../SKILL.md` 和 `../templates/map.md`，只向 Harness 提供对应“输入”，再对照“判据”检查输出；正常事件允许无回复或“暂无新的地图事件”，两者都不算地图。

计数单位统一为检查项。本文的 `T=总数/通过/失败/待做` 仅压缩输入，实际地图使用六列表；CI 未配置。除明确给出的验证外没有其他要求验证。Task 相关路径集合按路径去重，总地图取并集。

## 1. 小任务：完整路径

输入：首次 `/devmap`，尚未实施。需求“调整帮助文案”，Task T1“更新帮助”，预期结果“帮助页与说明一致”，预计修改 `src/help.md`、`docs/help.md`，无依赖。要求 L0 一项规范检查、L3 一项文案验收，均未运行；L1/L2 不适用。实际文件 0。

期望开工地图（不展开需求背景）：

**帮助更新 · 开工**

Task 1：待开始 1 · 开发中 0 · 测试中 0 · 已完成 0　｜　阻塞 0

| Task | 预期结果 | 状态 | 依赖 |
|---|---|---|---|
| 更新帮助 | 帮助页与说明一致 | 待开始 | 无 |

文件：预计 2 / 实际 0　｜　新增 0 / 删除 0　｜　范围变化：无

- 预计修改：`src/help.md`
- 预计修改：`docs/help.md`

计数单位：检查项　｜　自动检查：未配置

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查 | 1 | 0 | 0 | 1 |
| L1 | 单项验证（不适用） | 0 | 0 | 0 | 0 |
| L2 | 联合验证（不适用） | 0 | 0 | 0 | 0 |
| L3 | 最终验收 | 1 | 0 | 0 | 1 |

当前开工阻塞：无　｜　下一步：更新帮助文案。

判据：只有一张开工总地图，2 条完整路径；再次观察同一输入不生成新地图。将输入改为单一路径或加上 `docs/index.md` 时，分别得到 1 / 3 条完整路径和相应真实数量。

## 2. 中型任务：15 个文件

输入：T2 实现完成、要求 L0 1/1/0/0、L1 3/3/0/0，L2/L3 不适用，首次完成事件。计划与实际相同，均修改以下 15 个文件，无新增、删除、范围变化：

```text
src/a.md
src/b.md
src/c.md
src/d.md
src/e.md
src/f.md
src/g.md
src/h.md
src/i.md
src/j.md
src/k.md
src/l.md
src/m.md
src/n.md
src/o.md
```

判据：一张 T2 完成地图，原计划 15 / 实际 15，新增 0 / 删除 0，完整展示以上 15 条“修改：”路径，统一测试表 L0=1/1/0/0、L1=3/3/0/0、L2=L3=0（不适用）。不显示任何其他 Task。

## 3. 大型任务：121 个文件及逐级折叠

输入：T3 首次完成，计划与实际集合一致；根目录 `AGENTS.md` 为 M，其余全为 A。路径集合按以下有限展开式定义（闭区间，十进制数字补齐两位；无省略的未知项）：

```text
AGENTS.md
src/group{01..40}/item{01..03}.md
```

即 40 个不同目录各 3 个文件，总数 1 + 40 × 3 = 121，新增 120、删除 0。要求 L0=1/1/0/0、L1=120/120/0/0、L2=L3=0（不适用）。结果“121 个目标文件已更新”，范围无变化。

第一次按直接父目录折叠仍有 41 行，必须继续向 `src/` 合并。期望路径摘要只有 2 行：

- 修改：`AGENTS.md`（1 个文件）
- 新增：`src/`（120 个文件）

判据：完成地图保留原计划 121 / 实际 121、新增 120 / 删除 0，测试总数不随路径折叠变化；不得显示成“2 个文件”，不得截取前 30 个目录丢失其余计数。

边界变体：将 121 个路径全部置于根目录（`AGENTS.md` 加 `item{001..120}.md`），期望 `AGENTS.md` 1 + `./` 其余 120，不重复计入 AGENTS.md。将数量改为 30 个普通路径时逐个展示，31 个同目录普通路径时折叠。若计划为 30 个 `old/item{01..30}.md`、实际为这 30 个加 30 个 `new/item{01..30}.md`，整个路径区域按并集 60 折叠，显示 old 计划30/实际30、new 计划0/实际30，真实计划30/实际60。

## 4. 非结构化需求：自动整理

输入：无 checklist 的描述：“用户要能查看账单、下载 CSV，帮助页说明这两项功能。下载依赖账单数据就绪，帮助页可独立编写。账单与下载各做单项检查，再做下载联合验证；交付时按这三项功能做最终验收。”文件可读：`src/bills.ts`、`src/export.ts`、`docs/help.md`。尚未开发。

期望 Agent 自行整理至少以下三个 Task；等价拆分可接受，但结果、文件、验证和依赖不可丢失：

| Task | 预期结果 | 预计文件 | 所需测试 | 依赖 |
|---|---|---|---|---|
| T账单 查看账单 | 用户能看到账单 | src/bills.ts | L1 账单显示通过 | 无 |
| T下载 下载账单 | 用户能下载 CSV | src/export.ts | L1 导出通过、L2 下载联合验证通过 | T账单 |
| T帮助 更新帮助 | 两项功能说明齐全 | docs/help.md | L0 文案检查通过 | 无 |

另保留最终 L3 三项功能验收。判据：无需 负责人 补 checklist，输出一次包含 3 Task、预计 3 文件的开工总地图，不把素材逐段复述为地图。

## 5. 非决定性信息缺失：不反问

输入：场景 4 未指定 Task 名称、测试文件命名和断言组织；项目已有测试约定可供读取。

判据：Agent 自行取名与安排验证，仍可生成场景 4 的开工地图，不问“如何拆 Task / 测试如何命名”。未知预计路径可如实标未知，不能以此强制要求 负责人 决定普通实现细节。

## 6. 会改变结果的信息缺失：反问

输入：需求仅说“账单下载包含当前用户还是全公司账单还没决定”，现有资料无法推导，并且影响权限与产品行为。

期望询问：“账单下载应仅包含当前用户，还是全公司账单？”不得替 负责人 选择。依赖该决定的 Task 无法形成可执行结果时不宣称已可开工；其他明确 Task 仍可整理。若 T下载已经建立且现有授权内无法继续，则只生成一次确认阻塞事件地图，写明需要的业务决定。

判据：询问限于改变结果的缺口，不让 负责人 再填写完整 PRD。

## 7. 并发阻塞：独立 Task 继续

输入：T账单 已完成；T下载 测试中，缺少必须的外部下载服务权限，经核实当前授权无法取得；T帮助 开发中，无依赖。此前没有这个阻塞事件。

期望：T下载 保持“测试中 · 阻塞”，T帮助 可继续，不产生项目级“全体阻塞”或第五状态。状态数量为待开始0 / 开发中1 / 测试中1 / 已完成1，当前阻塞1（这些数量供内部核对，不额外生成总地图）。T帮助 后续进入测试不报告，完成时可单独输出其地图。

判据：依赖关系决定影响范围；不得阻止 T帮助。阻塞输出见场景 10；重复观察同一缺权原因不增加历史阻塞数。

## 8. 普通开发与失败修复：静默

输入：T帮助 从待开始开始，按下列顺序观察；所有失败都可在原授权内自行修复：

| 步骤 | 观察 | 内部状态 | 期望地图数 |
|---|---|---|---:|
| 1 | 开始任务 | 开发中 | 0 |
| 2 | 写入帮助页 | 开发中 | 0 |
| 3 | 执行文案检查 | 测试中 | 0 |
| 4 | 单次检查失败 | 测试中 | 0 |
| 5 | 自行修复文案 | 开发中 | 0 |
| 6 | 重新测试，结果尚未返回 | 测试中 | 0 |

判据：累计 0 张地图、0 次确认阻塞；不能把步骤 2 标为已完成。重复 `/devmap` 也不凭空产生阶段地图。

## 9. 单个 Task 完成：只报告变化项

输入：场景 8 后 T帮助 要求的唯一 L0 文案检查通过，文件 `docs/help.md` 为 M；计划1/实际1、新增0/删除0、范围无变化。T下载 仍在阻塞。首次观察到 T帮助 完成。

期望输出：

**更新帮助 · 已完成**

文件：原计划 1 / 实际 1　｜　新增 0 / 删除 0　｜　范围变化：无

- 修改：`docs/help.md`

计数单位：检查项　｜　自动检查：未配置

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查 | 1 | 1 | 0 | 0 |
| L1 | 单项验证（不适用） | 0 | 0 | 0 | 0 |
| L2 | 联合验证（不适用） | 0 | 0 | 0 | 0 |
| L3 | 最终验收（本 Task 不适用） | 0 | 0 | 0 | 0 |

结果：两项账单功能的帮助说明已完成。

判据：只有 T帮助 地图，不显示 T账单/T下载 看板，不因一个 Task 完成生成最终总地图；重复观察不再报告。原来的失败不累计到当前失败数。

## 10. 确认阻塞：具体决定与影响

输入：场景 7 首次确认 T下载 缺少服务权限。计划 `src/export.ts`，已修改同一文件；L1 一项已通过，L2 一项未运行。已写完下载实现但不能进行联合验证。

期望输出：

**下载账单 · 测试中 · 阻塞**

原因：缺少外部下载服务权限，当前授权无法取得。　｜　已完成：下载实现与单项验证。

影响：下载联合验证及最终交付。　｜　需要负责人决定：提供所需服务权限，或决定调整下载范围。

其他 Task：更新帮助可以继续。

文件：原计划 1 / 实际 1　｜　新增 0 / 删除 0　｜　范围变化：无

- 修改：`src/export.ts`

计数单位：检查项　｜　自动检查：未配置

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查（不适用） | 0 | 0 | 0 | 0 |
| L1 | 单项验证 | 1 | 1 | 0 | 0 |
| L2 | 联合验证 | 1 | 0 | 0 | 1 |
| L3 | 最终验收（本 Task 不适用） | 0 | 0 | 0 | 0 |

判据：一次阻塞地图，未运行验证不算通过；同一未解除阻塞重试仍只算一次。权限到位并开始验证不报告解除地图；若解除后再次失去权限且无法继续，则是第二个阻塞事件。

## 11. 完成后重开：失效证据

输入：T下载 后来所有要求验证通过，曾报告完成。最终 L3“下载包含所有应有记录”失败，证实遗漏最后一条记录，要求修复 `src/export.ts`。计划1/实际1，新增0/删除0。T账单/T帮助 仍有效。

期望输出：

**下载账单 · 重新打开 → 开发中**

原因：L3 验收发现下载遗漏记录，原完成结论失效。　｜　影响：下载结果与最终交付。

文件：原计划 1 / 实际 1　｜　新增 0 / 删除 0　｜　范围变化：无

- 修改：`src/export.ts`

计数单位：检查项　｜　自动检查：未配置

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查（不适用） | 0 | 0 | 0 | 0 |
| L1 | 单项验证 | 1 | 1 | 0 | 0 |
| L2 | 联合验证 | 1 | 1 | 0 | 0 |
| L3 | 最终验收（相关项） | 1 | 0 | 1 | 0 |

下一步：修复遗漏记录并重新验证下载结果。

判据：沿用 T下载 标识，完成数从3降为2，输出一次重开地图，不输出成功最终总地图。后续改动使旧 L1/L2 结果不再适用时更新为待做；相关验证再次全通过才能再次完成。

## 12. 开工只一次：重复与恢复

输入：场景 4 需求整理结束、开发尚未开始、尚未报告。随后再次观察相同 Task 集合，再次调用 `/devmap`。

判据：第一次一张开工总地图，后两次 0 张。若首次启用已在开发中，不补发开工地图；若上下文缺失历史是否报告，不猜测“从未报告”，不补发历史开工地图。状态仍可从可信材料重建。

## 13. 最终门槛：通过与不通过

输入：三 Task 均完成，实际修改 `src/bills.ts`、`src/export.ts`、`docs/help.md`，与原计划相同。L0=1/1/0/0、L1=2/2/0/0、L2=1/1/0/0，最终 L3 三项全部通过；当前阻塞0，独立历史阻塞事件1。最后 Task 完成地图已报告，最终总地图未报告。

期望输出：

**账单功能 · 全部完成**

Task 3 / 已完成 3　｜　当前阻塞 0 / 发生过的阻塞 1

文件：原计划 3 / 实际 3　｜　新增 0 / 删除 0　｜　范围变化：无

- 修改：`src/bills.ts`
- 修改：`src/export.ts`
- 修改：`docs/help.md`

计数单位：检查项　｜　自动检查：未配置

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查 | 1 | 1 | 0 | 0 |
| L1 | 单项验证 | 2 | 2 | 0 | 0 |
| L2 | 联合验证 | 1 | 1 | 0 | 0 |
| L3 | 最终验收 | 3 | 3 | 0 | 0 |

最终结果：账单查看、CSV 下载和帮助说明均满足验收要求。

判据：一次最终总地图；重复输入不再报告。以下独立变体均不得生成成功最终地图：一个 Task 尚在测试中、L3 未运行、L3 不可访问、L3 失败、尚有未解除阻塞。失败证明已完成 Task 失效时，改走场景11。最后 Task 完成与最终门槛同时首次成立时，两种合格事件各一张，随后重复观察均不报告。最终报告后被证实失效，须撤销结论并在重新满足门槛后明确报告修正结果。

## 14. 共同呈现与计数不变量

输入：依次使用场景 1、9、10、11、13 的五类地图；再使用以下计数变体。

- 共享文件：T甲 计划/实际 `{src/a.md, docs/shared.md}`，T乙 `{src/b.md, docs/shared.md}`，各 Task 显示2，总地图显示3，不是4。
- 文件增删与范围：计划修改 `src/a.md`，实际修改它并新增 `docs/new.md`、删除 `docs/old.md`，总实际3、新增1/删除1，显示“修改：src/a.md”“新增：docs/new.md”“删除：docs/old.md”及新增范围的原因，不将原计划改成3。
- 验证未知：CI 不可访问、L2 数量无法枚举，则 L2 使用未知并说明原因；不得写“通过”。已知一项待运行则显示1/0/0/1。
- 重跑：同一 L1 检查失败后通过，再在 CI 上通过，仍总数1/通过1/失败0/待做0。旧 HEAD 的 CI 通过不能用于新 HEAD。主地图只写“自动检查：已通过 · 旧版本，当前版本待确认”；当前 HEAD 的通过结果已核实后才写“自动检查：已通过 · 当前版本”，不展示 SHA / HEAD 值。

判据：每类地图均含真实文件数、路径摘要、同一张六列 L0～L3 表，用途为中文；阻塞/重开地图也保留简短数值摘要。总数已知时满足各测试行守恒与文件集合守恒；无完整 PRD、命令、日志、无关 Task 或旧 ATTENTION/SIZE/COMPLEXITY 主地图字段。五类模板均有明确结果或当前影响。输出保留 Task、L0～L3；文件状态使用修改 / 新增 / 删除，自动检查使用自然中文，业务决策使用“需要负责人决定”，不输出 A/M/D、SHA/HEAD 或 Human。

验收默认行为：**开工前一张总地图；开发过程中只在 Task 完成、确认阻塞或重新打开时报告；全部完成后再生成一张最终总地图。**
