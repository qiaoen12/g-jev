# 报告协议 Mock 验收

本文包含 14 项语义验收与 6 项视觉验收。所有输入、路径和结果均为固定 Mock；它们不是当前仓库开发记录，也不声称执行了真实业务测试。重放时先读 devmap/SKILL.md 和 devmap/templates/map.md，再把各项“输入”提供给 Skill，按“期望输出”和“判据”验收。

计数单位统一为检查项。文中的 T=总数/通过/失败/待做 仅压缩输入；实际地图使用统一六列表。仓库自动检查未配置。Task 路径集合去重，总地图按全体 Task 路径并集计算。用户地图不得显示预期结果、完成条件或依赖明细。

## 1. 小任务：完整路径与开工地图

输入：首次调用 /devmap，需求“调整帮助文案”，Task T1“更新帮助”，预期结果“帮助页与说明一致”，完成条件为两处帮助说明一致，预计修改 src/help.md、docs/help.md，无依赖。要求 L0 一项规范检查、L3 一项文案验收，均未运行；L1/L2 不适用。尚未实施，实际文件 0。

<details>
<summary>期望输出</summary>

# DevMap · 帮助更新

| 总任务 | 已完成 | 预计文件 | 当前阻塞 |
|---:|---:|---:|---:|
| 1 | 0 | 2 | 0 |

| 待开始 | 开发中 | 测试中 | 已完成 |
|---|---|---|---|
| 更新帮助 | — | — | — |

## 预计改动

预计修改文件：2
新增依赖：0
范围变化：无

    src/
    └── help.md

    docs/
    └── help.md

## 测试

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查 | 1 | 0 | 0 | 1 |
| L1 | 单项验证（不适用） | 0 | 0 | 0 | 0 |
| L2 | 联合验证（不适用） | 0 | 0 | 0 | 0 |
| L3 | 最终验收 | 1 | 0 | 0 | 1 |

当前问题：无
下一步：更新帮助文案。

</details>

判据：开工看板只有四个规定的状态列，2 个真实文件按目录树展示。再次观察同一输入不生成新地图。改为单一路径或增加 docs/index.md 时，路径树和真实文件数相应更新为 1 或 3。

## 2. 中型任务：15 个文件

输入：T2 实现完成，计划与实际相同，修改 src/a.md 至 src/o.md 共 15 个文件，无新增、删除或范围变化。L0=1/1/0/0、L1=3/3/0/0，L2/L3 不适用。

判据：仅一张 T2 完成地图。计划 15 / 实际 15；新增 0 / 删除 0；15 个实际路径按 src/ 目录树展示，统一测试表 L0=1/1/0/0、L1=3/3/0/0、L2=L3=0（不适用）。不得显示其他 Task。树中的路径须逐一对应输入，真实计数与标题一致，路径区域不超过约 30 行。

## 3. 大型任务：121 个文件及逐级折叠

输入：T3 首次完成，计划与实际集合一致；根目录 AGENTS.md 为修改，其余全为新增。路径集合由 AGENTS.md 和 src/group01/item01.md 至 src/group40/item03.md 组成，每个 group 目录恰有 3 个文件，总数 1 + 40 × 3 = 121，新增 120、删除 0。要求 L0=1/1/0/0、L1=120/120/0/0、L2=L3=0（不适用）。

期望路径摘要：

- 修改：AGENTS.md（1 个文件）
- 新增：src/（120 个文件）

判据：计划 / 实际均为 121；目录汇总为 120，加上单列文件 1，完整还原 121。不得显示为“2 个文件”，不得截断目录或丢失计数。

边界变体：121 个路径全部置于根目录时显示 AGENTS.md 1 个和根目录其余 120 个，不重复计算 AGENTS.md。普通文件数为 30 时尽量逐路径展示，31 个同目录文件时折叠。计划 30 个 old/item 文件、实际再增加 30 个 new/item 文件时，展示项按计划 / 实际区分，真实计划 30、实际 60。

## 4. 非结构化需求：自动整理 Task

输入：无 checklist 的描述：“用户要能查看账单、下载 CSV，帮助页说明这两项功能。下载依赖账单数据就绪，帮助页可独立编写。账单与下载各做单项检查，再做下载联合验证；交付时按这三项功能做最终验收。”文件可读：src/bills.ts、src/export.ts、docs/help.md。尚未开发。

期望内部 Task 数据；它不作为用户地图：

| Task | 预期结果 | 完成条件 | 预计文件 | 所需验证 | 依赖 |
|---|---|---|---|---|---|
| T账单 查看账单 | 用户能看到账单 | 账单内容正确显示 | src/bills.ts | L1 账单显示通过 | 无 |
| T下载 下载账单 | 用户能下载 CSV | 导出完整且可读取 | src/export.ts | L1 导出、L2 联合验证通过 | T账单 |
| T帮助 更新帮助 | 两项功能说明齐全 | 帮助页与交付功能一致 | docs/help.md | L0 文案检查通过 | 无 |

另保留最终 L3 三项功能验收。开工地图有 3 个 Task、预计 3 个文件；四列看板仅显示 Task 名称，不把上表渲染为用户地图。

判据：无需负责人补 checklist，不把素材逐段复述为地图；预期结果、完成条件、依赖仍保留在内部数据。

## 5. 非决定性信息缺失：不反问

输入：场景 4 未指定 Task 名称、验证文件命名和断言组织；项目已有验证约定可供读取。

判据：Agent 自行取名与安排验证，仍可生成场景 4 的开工地图，不问“如何拆 Task / 验证如何命名”。未知预计路径可如实标未知，不能以此要求负责人决定普通实现细节。

## 6. 会改变结果的信息缺失：反问

输入：需求只说“账单下载包含当前用户还是全公司账单还没决定”，现有资料无法推导，并且影响权限与产品行为。

期望询问：“账单下载应仅包含当前用户，还是全公司账单？”不得替负责人选择。依赖该决定的 Task 无法形成可执行结果时不宣称已可开工；其他明确 Task 仍可整理。若 T下载已建立且现有授权内无法继续，则只生成一次确认阻塞事件地图，写明需要的业务决定。

判据：询问限于改变结果的缺口，不让负责人再填写完整 PRD。

## 7. 并发阻塞：独立 Task 继续

输入：T账单已完成；T下载测试中，缺少必须的外部下载服务权限，经核实当前授权无法取得；T帮助开发中、无依赖。此前没有该阻塞事件。

判据：T下载保持“测试中 · 阻塞”，T帮助可继续；不产生项目级全体阻塞或第五状态。状态数量为待开始 0 / 开发中 1 / 测试中 1 / 已完成 1，当前阻塞 1（供内部核对）。T帮助进入测试不报告，完成时可单独输出其地图。重复观察同一缺权原因不增加历史阻塞数。

## 8. 普通开发与失败修复：静默

输入：T帮助从待开始开始，按以下顺序观察；所有失败都可在原授权内自行修复：

| 步骤 | 观察 | 内部状态 | 地图数 |
|---|---|---|---:|
| 1 | 开始任务 | 开发中 | 0 |
| 2 | 写入帮助页 | 开发中 | 0 |
| 3 | 执行文案检查 | 测试中 | 0 |
| 4 | 单次检查失败 | 测试中 | 0 |
| 5 | 自行修复文案 | 开发中 | 0 |
| 6 | 重新验证，结果尚未返回 | 测试中 | 0 |

判据：累计 0 张地图、0 次确认阻塞；不能把步骤 2 标为已完成。重复调用 /devmap 也不产生阶段地图。

## 9. 单个 Task 完成：只报告变化项

输入：T帮助要求的唯一 L0 文案检查通过，docs/help.md 已修改；计划 1 / 实际 1，新增 0 / 删除 0，范围无变化。T下载仍阻塞。首次观察到 T帮助完成。

<details>
<summary>期望输出</summary>

# DevMap · 更新帮助 · 已完成

文件：计划 1 / 实际 1
新增：0 · 删除：0 · 范围变化：无

修改：

    docs/
    └── help.md

## 测试

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查 | 1 | 1 | 0 | 0 |
| L1 | 单项验证（不适用） | 0 | 0 | 0 | 0 |
| L2 | 联合验证（不适用） | 0 | 0 | 0 | 0 |
| L3 | 最终验收（本 Task 不适用） | 0 | 0 | 0 | 0 |

结果：帮助说明已更新并通过要求的验证。

</details>

判据：只有 T帮助地图，不显示 T账单 / T下载 或其他 Task；不因一个 Task 完成生成最终总地图。重复观察不再报告。普通失败已修复，不累计为当前失败。

## 10. 确认阻塞：具体原因与影响

输入：首次确认 T下载缺少服务权限。计划 src/export.ts，已修改同一文件；L1 一项已通过，L2 一项未运行。下载实现已完成，但无法进行联合验证。T帮助可以继续。

<details>
<summary>期望输出</summary>

# DevMap · 下载账单 · 测试中 · 阻塞

原因：缺少外部下载服务权限，当前授权无法取得。
已完成：下载实现与单项验证。
影响：下载联合验证和最终交付。
需要负责人决定：提供所需服务权限，或决定调整下载范围。
其他 Task：更新帮助可以继续。

文件：计划 1 / 实际 1
新增：0 · 删除：0 · 范围变化：无

修改：

    src/
    └── export.ts

## 测试

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查（不适用） | 0 | 0 | 0 | 0 |
| L1 | 单项验证 | 1 | 1 | 0 | 0 |
| L2 | 联合验证 | 1 | 0 | 0 | 1 |
| L3 | 最终验收（本 Task 不适用） | 0 | 0 | 0 | 0 |

</details>

判据：一次阻塞地图，未运行验证不算通过；同一未解除原因重试仍只算一次。权限到位并开始验证不报告解除地图；解除后再次失去权限且无法继续，则是第二个阻塞事件。

## 11. 完成后重开：失效证据

输入：T下载曾经所有要求验证通过并报告完成。最终 L3“下载包含所有应有记录”失败，证实遗漏最后一条记录，要求修复 src/export.ts。计划 1 / 实际 1，新增 0 / 删除 0；T账单与 T帮助仍有效。

<details>
<summary>期望输出</summary>

# DevMap · 下载账单 · 重新打开 → 开发中

原因：最终验收发现下载遗漏记录，原完成结论失效。
影响：下载结果和最终交付。

文件：计划 1 / 实际 1
新增：0 · 删除：0 · 范围变化：无

修改：

    src/
    └── export.ts

## 测试

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查（不适用） | 0 | 0 | 0 | 0 |
| L1 | 单项验证 | 1 | 1 | 0 | 0 |
| L2 | 联合验证 | 1 | 1 | 0 | 0 |
| L3 | 最终验收（相关项） | 1 | 0 | 1 | 0 |

下一步：修复遗漏记录并重新验证下载结果。

</details>

判据：沿用 T下载标识，完成数从 3 降为 2，输出一次重开地图，不输出成功最终总地图。后续改动使旧 L1/L2 结果不再适用时更新为待做；相关验证再次全部通过才能再次完成。

## 12. 开工只一次：重复与恢复

输入：场景 4 需求已整理为 Task，开发尚未开始，尚未报告。随后再次观察相同 Task 集合，再次调用 /devmap。

判据：第一次输出一张开工总地图，后两次输出 0 张。首次启用时若开发已经开始，不补发开工地图；上下文缺失历史是否报告时，不猜测并补发历史地图。状态仍可从可信材料重建。

## 13. 最终门槛：通过与不通过

输入：三 Task 均完成，实际修改 src/bills.ts、src/export.ts、docs/help.md，与原计划相同。L0=1/1/0/0、L1=2/2/0/0、L2=1/1/0/0，最终 L3 三项全部通过；当前阻塞 0，独立历史阻塞事件 1。最后 Task 完成地图已报告，最终总地图未报告。

<details>
<summary>期望输出</summary>

# DevMap · 账单功能 · 全部完成

| 总任务 | 已完成 | 实际文件 | 当前阻塞 |
|---:|---:|---:|---:|
| 3 | 3 | 3 | 0 |

计划文件：3 → 实际文件：3
新增：0 · 删除：0
范围变化：无
发生过的阻塞：1

    docs/
    └── help.md

    src/
    ├── bills.ts
    └── export.ts

## 测试

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查 | 1 | 1 | 0 | 0 |
| L1 | 单项验证 | 2 | 2 | 0 | 0 |
| L2 | 联合验证 | 1 | 1 | 0 | 0 |
| L3 | 最终验收 | 3 | 3 | 0 | 0 |

最终结果：账单查看、CSV 下载和帮助说明均满足验收要求。

</details>

判据：所有 Task、最终 L3 和阻塞条件满足后输出一次最终总地图；不重复输出空看板或 Task 明细。以下变体均不得输出成功最终地图：一个 Task 尚在测试中、L3 未运行、L3 不可访问、L3 失败、尚有未解除阻塞。失败证明已完成 Task 失效时改走场景 11。最终报告后被证实失效，须撤销结论并在重新满足门槛后明确报告修正结果。

## 14. 共同呈现与计数不变量

输入：依次核对场景 1、9、10、11、13 的五类地图，再核对以下计数变体。

- 共享文件：T甲计划 / 实际 {src/a.md, docs/shared.md}，T乙 {src/b.md, docs/shared.md}；各 Task 显示 2，总地图显示 3，不是 4。
- 文件增删与范围：计划修改 src/a.md；实际修改它并新增 docs/new.md、删除 docs/old.md；计划文件 1、实际文件 3、新增 1 / 删除 1。分别用中文动作展示文件，不将实际结果回填到原计划。
- 验证未知：仓库自动检查不可访问、L2 数量无法枚举时，L2 写未知并说明原因，不得写“通过”。若已知一项待运行则显示 1/0/0/1。
- 重跑：同一 L1 检查失败后修复通过，仍按总数 1 / 通过 1 / 失败 0 / 待做 0。旧版本检查结果不能证明当前版本通过；需要显示状态时用“自动检查”及中文状态，不展示版本标识。

判据：五类地图都有真实文件数、路径树或目录摘要和同一张六列 L0～L3 表；阻塞 / 重开地图保留简短数值摘要。测试数与文件集合守恒。地图不含完整 PRD、命令、日志或无关 Task，不使用旧主地图字段或过程报告。

## 15. 开工总地图：四个核心数字与四列看板

固定输入“初始化流程”：共有 6 个 Task，全部待开始；预计修改 12 个文件；当前“初始化入口”有 1 个开工阻塞。预计文件为 bin/bootstrap、bin/machine-check、tools/bootstrap/ 下 6 个文件和 tests/ 下 4 个文件。新增依赖 0，范围变化无。L0=8/2/0/6、L1=5/0/0/5、L2=2/0/0/2、L3=1/0/0/1。当前问题是等待独立审核批准。

<details>
<summary>期望实际渲染</summary>

# DevMap · 初始化流程

| 总任务 | 已完成 | 预计文件 | 当前阻塞 |
|---:|---:|---:|---:|
| 6 | 0 | 12 | 1 |

| 待开始 | 开发中 | 测试中 | 已完成 |
|---|---|---|---|
| 初始化入口（阻塞） | — | — | — |
| 机器配置 |  |  |  |
| 失败保护 |  |  |  |
| 仓库初始化 |  |  |  |
| 兼容检查 |  |  |  |
| 最终验收 |  |  |  |

## 预计改动

预计修改文件：12
新增依赖：0
范围变化：无

    bin/
    ├── bootstrap
    └── machine-check

    tools/bootstrap/
    ├── config.py
    ├── env.py
    ├── init.py
    ├── checks.py
    ├── paths.py
    └── repair.py

    tests/
    ├── test_init.py
    ├── test_config.py
    ├── test_checks.py
    └── test_repair.py

## 测试

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查 | 8 | 2 | 0 | 6 |
| L1 | 单项验证 | 5 | 0 | 0 | 5 |
| L2 | 联合验证 | 2 | 0 | 0 | 2 |
| L3 | 最终验收 | 1 | 0 | 0 | 1 |

当前问题：等待独立审核批准
下一步：批准后开始可执行 Task。

</details>

判据：四个核心数字紧邻标题，随后是四列看板。6 个 Task 各占独立一行，名称都位于正确状态列；空列仅首行显示“—”，后续行留空；初始化入口带“（阻塞）”标记；格内没有预期结果、完成条件或依赖。预计文件真实数为 12，路径树叶计数相加为 12。

### 排版回归：同状态多个 Task 与空列

固定输入：仅核对开工看板区域；三个 Task 均待开始，分别为“更新工作流配置”（阻塞）、“验证语法与既有检查”、“验证受控失败和恢复”。

期望实际渲染：

| 待开始 | 开发中 | 测试中 | 已完成 |
|---|---|---|---|
| 更新工作流配置（阻塞） | — | — | — |
| 验证语法与既有检查 |  |  |  |
| 验证受控失败和恢复 |  |  |  |

判据：三个 Task 对应三行；每行只有一个 Task，三个空状态列仅首行有“—”。任何把多个 Task 用 `<br>`、字面量 `\n` 或其他 HTML 换行合并到一个单元格的输出均判失败。长名称在窄显示区域允许自然换行，不得手工插入 HTML。

状态分布变体（仅核对布局，不产生新的地图触发事件）：将第二个 Task 置于开发中、第三个 Task 置于测试中，仍须各占独立一行；只有已完成列为空，首行显示“—”。期望看板：

| 待开始 | 开发中 | 测试中 | 已完成 |
|---|---|---|---|
| 更新工作流配置（阻塞） |  |  | — |
|  | 验证语法与既有检查 |  |  |
|  |  | 验证受控失败和恢复 |  |

## 16. 看板名称与内部 Task 字段

输入：场景 15 的六个 Task 内部记录分别保留预期结果、完成条件、所需验证和依赖。例如“机器配置”的预期结果是发现缺失配置，完成条件是只报告可修复问题，依赖“初始化入口”。这些是 Task 整理与完成判断所需字段。

判据：场景 15 的四列中只渲染“初始化入口、机器配置、失败保护、仓库初始化、兼容检查、最终验收”六个名称，初始化入口附“（阻塞）”标记。不得在格子或其他默认看板区域显示预期结果、依赖或完成条件。内部 Task 数据仍完整保留这些值；字段保留不等于默认呈现。

## 17. 最终总地图：计划与实际

固定输入“初始化流程”：6 个 Task 全部完成，计划 8 个文件、实际 10 个文件，新增 2、删除 0；范围变化为增加公共配置同步；历史阻塞 1、当前阻塞 0。实际文件是 bin/ 下 2 个、tools/bootstrap/ 下 6 个、tests/ 下 2 个。L0=8/8/0/0、L1=5/5/0/0、L2=2/2/0/0、L3=1/1/0/0。

<details>
<summary>期望实际渲染</summary>

# DevMap · 初始化流程 · 全部完成

| 总任务 | 已完成 | 实际文件 | 当前阻塞 |
|---:|---:|---:|---:|
| 6 | 6 | 10 | 0 |

计划文件：8 → 实际文件：10
新增：2 · 删除：0
范围变化：有，增加公共配置同步
发生过的阻塞：1

    bin/
    ├── bootstrap
    └── machine-check

    tools/bootstrap/
    ├── config.py
    ├── env.py
    ├── init.py
    ├── checks.py
    ├── paths.py
    └── repair.py

    tests/
    ├── test_init.py
    └── test_checks.py

## 测试

| 等级 | 用途 | 总数 | 通过 | 失败 | 待做 |
|---|---|---:|---:|---:|---:|
| L0 | 规范检查 | 8 | 8 | 0 | 0 |
| L1 | 单项验证 | 5 | 5 | 0 | 0 |
| L2 | 联合验证 | 2 | 2 | 0 | 0 |
| L3 | 最终验收 | 1 | 1 | 0 | 0 |

最终结果：6 个 Task 全部完成，最终验收通过。

</details>

判据：最终地图使用计划与实际摘要，不重复四列空看板、所有 Task 明细或完整开发过程；计划 8 与实际 10 的真实路径树保持准确。

## 18. 路径呈现：小集合树形、大集合折叠

小集合固定输入 5 个预计文件：.github/workflows/jev-docs.yml、docs/README.md、docs/architecture.md、docs/usage.md、scripts/check-docs.sh。期望路径区域如下：

    .github/
    └── workflows/
        └── jev-docs.yml

    docs/
    ├── README.md
    ├── architecture.md
    └── usage.md

    scripts/
    └── check-docs.sh

判据：标题仍写预计文件 5；目录树代表 5 个真实文件。不得改成 5 条相互无关的散列路径。

大集合固定输入：src/ 71 个、tests/ 38 个、.github/ 8 个、docs/ 9 个文件，总数 126。期望显示：

    预计修改文件：126

    src/                   71 个文件
    tests/                 38 个文件
    .github/                 8 个文件
    docs/                    9 个文件

判据：目录数相加仍为 126；路径展示项数为 4，不得把真实文件数写成 4。折叠层级加深时目录集合仍不重叠、文件计数不变。

## 19. 中文用户可见层

输入：逐项检查场景 1、9、10、11、13、15、17 的渲染正文，不检查内部 Task 记录或本节判据。

判据：地图的文件状态写“新增 / 修改 / 删除”，检查状态写“自动检查”及中文结果，业务决策角色写“负责人”。不得默认显示 A/M/D、HEAD、SHA、CI、SUCCESS、FAILURE、Human 等开发缩写。Task 和 L0～L3 可以保留。路径本身作为文件标识保持原样。

## 20. 五类固定 Mock 地图的实际渲染

固定渲染集如下；这些样例正文就是实际渲染结果，不是字段存在检查：

| 地图类别 | 固定 Mock 渲染 |
|---|---|
| 开工总地图 | 场景 15：初始化流程，6 个 Task、12 个预计文件、1 个当前阻塞 |
| 最终总地图 | 场景 17：初始化流程，计划 8 / 实际 10 个文件 |
| Task 完成地图 | 场景 9：更新帮助，仅报告 docs/help.md |
| Task 阻塞地图 | 场景 10：下载账单，缺少下载服务权限 |
| Task 重新打开地图 | 场景 11：下载遗漏记录，恢复开发 |

人工比对当前 Issue #8 第 8.1～8.8 节的模板结构、核心数字、列顺序、隐藏内部字段、计划 / 实际摘要、路径树和折叠计数、统一测试表、简洁中文与结果句。判定标准是最终渲染外观；任一类型错用 Task 明细表、显示其他 Task、丢失真实文件数、重复空看板、展开内部字段或暴露不必要的开发标识都判失败。

验收结论：**开工前一张总地图；开发过程中只在 Task 完成、确认阻塞或重新打开时报告；全部 Task 与最终验收完成后再输出计划与实际总地图。**
